#include "MemoManager.h"

MemoManager::MemoManager(QObject* parent) : QObject(parent) {

}

MemoManager::~MemoManager() {

}

bool MemoManager::saveRecord(const QString& strRecord) {
    // Todo(wangwenxi): to be continue..
    return true;
}

bool MemoManager::deleteRecord(const QString& strRecord) {

    return true;
}
